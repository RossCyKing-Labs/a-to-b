---
// Word→PDF dropped from the live surface area. Microsoft Word and Google Docs
// both already convert .docx to PDF with perfect fidelity for free — we can't
// match that in-browser without 150 MB+ of WASM. Better to focus AtoB on tools
// where we provide unique value (image conversion, PDF manipulation).
//
// The component + library code remain in the repo for future restoration if
// browser-side conversion tech improves to viable fidelity.
return Astro.redirect('/');
---
