// Deprecated: legacy mammoth-based docx renderer.
// We migrated to docx-preview (see docxRender.ts) and then dropped
// Word→PDF entirely from the live surface. Kept as an empty stub so any
// stale tooling reference still resolves without pulling in mammoth.
export {};
