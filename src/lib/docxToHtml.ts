/**
 * Deprecated. Replaced by src/lib/docxRender.ts (docx-preview based).
 *
 * Kept as an empty stub so any stale imports during a build fail loudly
 * instead of silently. Delete this file in a future cleanup once all
 * tooling is verified green.
 */
export {};
